# 기프큐(GiftCue) 사업 기획서 및 앱 개발 명세서 (PRD)

**구독형 기업·MICE 기념품 플레이스먼트 및 턴키 B2B SaaS**

> 매년, 예산에 맞춰, 지역/한국 적합성까지 고려해 플레이스먼트된 기념품 후보를 알아서 가져다주는 구독 서비스.
> 큐레이션부터 웰컴패키지, 예산 장부 관리까지 MICE 담당자의 탐색/포장/회계 부담을 없앤다. (참여자 개별 다중 배송은 Phase 2 확장 기능)

| 항목 | 내용 |
|---|---|
| 문서 성격 | 사업 기획서 + 앱 개발용 상세 스펙(PRD) 통합본 |
| 타겟 고객 | [실무자] MICE·기업 행사 담당, 마케팅/총무 담당 / [승인자] 예산 통제 권한자 |
| 핵심 모델 | 월 구독권(샘플 티켓 포함) + 상품 마진 + 턴키 패키지 수수료 (다중 배송 수수료는 Phase 2) |
| 플랫폼 | **모바일 우선 웹앱 (Next.js) / Vercel 배포 (MVP 데모용, 네이티브 앱 아님)** |
| 버전 | **v7.0 (v6.3 + 실제 구현 반영본 As-Built + 신규 기능: 실제 뮤지엄샵 데이터 · KREAM 디자인 · 부산 기본 시나리오 · Fit 정렬 필터 · 주문 방식 2종[직접 담기/자동 큐레이션])** |

---

## 0. 통합 변경 이력 (v5.0 → … → v6.3 → v7.0)

*(v5.0~v6.3 이력은 원본과 동일. 아래는 v7.0 신규 이력.)*

**v6.3 → v7.0** 은 문서 단계에서 실제 **구현(MVP 데모)** 단계로 넘어오며, 지금까지 코드로 만들어진 것을 PRD에 역반영(As-Built)하고 몇 가지 기능을 새로 추가한 버전이다.

1. **데이터 소스 확정 — 국립박물관문화상품(museumshop.or.kr) 실제 상품.** v6.3까지 상품은 컨셉 더미였으나, v7.0에서는 뮤지엄샵의 **실제 상품 14종**(반가사유상 미니어처, 금동대향로 미니어처, 신라 금관 손수건, 경주 다보탑 키트, 단청 블루투스 키보드, 대동여지도 시리즈, 동자석 인형 키링, 곤룡포 타월/스티커, 동백꽃 손수건, 분청사기 물고기 가제수건 등)의 실제 상품명·가격·이미지를 사용한다. 이미지는 **전부 `/public/products`에 로컬 저장**해 데모 중 네트워크로 인한 깨짐이 없도록 했다(외부 이미지 의존성 0).

2. **디자인 레퍼런스 — KREAM.** 화이트 캔버스 + near-black 텍스트 + 포인트 레드(`#EF6253`) 1색 + 정사각 상품 카드 + 하단 라인 아이콘 탭바 + Pretendard 서체의 미니멀·에디토리얼 룩으로 통일했다.

3. **부산 기본 시나리오 + 듀얼 프로젝트.** 기본 프로젝트를 **부산(mixed)** 으로 두고, 부산 시화(市花)인 **동백꽃 손수건**과 해양도시 모티프인 **분청사기 물고기 가제수건**을 부산 로컬 상품으로 추가했다. 두 번째 프로젝트는 **경주(domestic)** 로 설정해, 프로젝트를 전환하면 로컬 핏 스타 상품과 한국 핏 노출 여부가 함께 바뀌는 것을 시연할 수 있다.

4. **[신규] Fit 정렬 필터.** 홈 피드를 **통합(로컬+한국)/로컬 핏만/한국 핏만** 3가지 기준으로 정렬할 수 있는 필터를 추가했다. 기본값은 통합 내림차순(=가장 잘 맞는 상품이 위). 국내 전용(domestic) 프로젝트에서는 한국 핏이 의미가 없으므로 '한국 핏' 정렬 옵션이 비활성화되고 통합으로 폴백된다. (7.2.2 참고)

5. **[신규] 주문 방식 2종.** 상품을 받는 방법을 두 가지로 나눴다. (a) **직접 담기** — 원하는 상품을 장바구니에 담아 한 번에 주문, (b) **자동 큐레이션** — 사용자가 미리 지정한 카테고리에서 GiftCue가 무작위로 골라 발송. 이는 "알아서 골라주는 구독"이라는 핵심 USP를 실제 주문 플로우로 구현한 것이다. (7.4 참고)

6. **플랫폼 결정 — 모바일 웹앱.** 데모는 회의 중 폰에서 링크 클릭 즉시 열려야 하고 백엔드/결제/인증이 전부 목이므로, 네이티브 앱이 아닌 **모바일 우선 웹앱(Next.js + Vercel)** 으로 확정했다.

7. **컴포넌트 전면 분리 + 한국어 주석.** 유지보수를 위해 모든 컴포넌트를 기능별 파일 1개당 1개로 분리하고, 모든 파일/주요 로직에 한국어 설명 주석을 달았다. (7.5 참고)

---

## 1. 배경 및 문제 정의 (Problem)

MICE(회의·인센티브·컨벤션·전시)와 기업 마케팅 현장에서 담당자의 일상은 대략 이렇다.

- **P1. 반복 탐색 노동:** 행사마다 "이번엔 뭘 주지?"를 0에서 고민하며 발품과 장거리 미팅을 반복한다.
- **P2. 예산 및 회계 증빙:** 확정된 예산 내 최적 조합을 찾기 어렵고, 업체별 견적 비교와 지출 기안·정산이 번거롭다.
- **P3. 스토리 부재:** 뻔한 기성품으로는 행사 참가자에게 지역적 특색이나 '한국다움'을 전달하기 어렵다.
- **P4. 물류·포장:** 물건을 받아 직접 포장(가내수공업)하느라 리소스가 낭비된다.

**솔루션:** 예산·취향·지역/한국 적합성 기반 자동 큐레이션 구독 + 샘플 티켓 + 턴키 웰컴패키지 + 예산 장부 앱.
(※ 참여자 개별 다중 배송 물류는 Phase 2 고도화 단계에서 지원)

---

## 2. 타겟 고객 & 대표 페르소나 (Target)

| 세그먼트 | 특징 및 가치 제안 |
|---|---|
| **구매 실무자** (이수현, 36) | MICE·기업 행사 담당. 행사마다 기념품 기획·샘플링·포장·발주 반복. <br>💬 *"야근과 장거리 미팅 없이, 앱에서 샘플 티켓만 누르세요."* |
| **예산 승인자** (재무/총무) | 예산 통제 및 투명한 지출 증빙 필요. <br>💬 *"예산 장부 자동화, 비교견적서 원클릭 다운로드로 회계를 간소화합니다."* |

> **참고 — 최종 수혜자(End Beneficiary):** 실제 구매/결정은 위 페르소나가 하지만, 최종적으로 기념품을 받는 사람은 MICE 행사에 참가하는 **외국인 방문객**인 경우가 많다. 이 점을 반영해 '한국 핏 지수'(3장 USP, 7.2.1)를 제공한다.

---

## 3. 기프큐만의 핵심 차별화 (USP - Why GiftCue?)

1. **지역/한국 적합성 매칭 (로컬 핏 & 한국 핏 지수):** 단순 가격·카테고리 추천이 아니라, 담당자가 프로젝트 생성 시 입력한 **행사 지역·테마·참가자 유형**을 기준으로 상품 적합도를 '핏(Fit) 지수'로 계산해 제공한다. 로컬 핏은 "이 지역/행사에 맞는가", 한국 핏은 "외국인에게 '한국'을 전달하는가"를 각각 점수화한다.
2. **[신규] Fit 정렬 필터:** 위 두 지수를 **통합/로컬만/한국만** 기준으로 정렬해, 담당자가 원하는 관점(지역성 우선 vs 한국성 우선)으로 후보를 재배열할 수 있다.
3. **[신규] 주문 방식 2종 — 직접 담기 & 자동 큐레이션:** (a) 원하는 상품을 골라 장바구니로 주문하거나, (b) 카테고리만 미리 지정하면 GiftCue가 매번 그 안에서 **알아서 골라 발송**하는 구독형 발송. "고민 없이 받아보는 선물 구독"을 실제 플로우로 구현했다.
4. **A to Z 턴키 웰컴 키트:** 단순 낱개 배송을 넘어 브랜딩 박스 포장·웰컴패키지 옵션을 제공해 행사 준비를 끝낸다. (참여자별 개별 다중 배송은 Phase 2)
5. **프로젝트 예산 장부 자동화:** 발주가 곧 예산 차감으로 실시간 반영되고, 지출 내역이 자동 기록된다. 비교견적서 자동 생성도 지원.
6. **ESG 성과 리포트:** 친환경 상품 발주 시 플라스틱/탄소 절감 기여를 인포그래픽으로 시각화(대기업 협력사 ESG 평가 대응용).

---

## 4. 비즈니스 모델: 구독 티어 (BM)

구독료로 기초 운영비·샘플 리스크를 방어하고, 상품 마진으로 수익을 낸다.

| 티어 | 요금(월) | 혜택 및 기능 |
|---|---|---|
| **Basic** | 월 39,000원 | - 월 1회 무료 샘플 키트(최대 3개 상품) **티켓 🎫**<br>- 기본 큐레이션 피드 및 비교견적서 다운로드<br>- 단일 프로젝트 예산 장부 |
| **Pro** | 월 99,000원 | - 월 2회 무료 샘플 **티켓 🎫🎫**<br>- 웰컴 키트 턴키 지원 · 자동 큐레이션 발송<br>- 무제한 프로젝트 예산 장부 & ESG 리포트 |

> 주문은 **직접 담기(장바구니)** 와 **자동 큐레이션(카테고리 지정 발송)** 두 방식 모두 지원한다.
> Phase 2에서 참여자 개별 다중 배송이 추가되면 Pro 티어 혜택 또는 별도 부가 수수료 항목으로 편입될 예정.

---

## 5. 시장 규모 및 경쟁 분석 (Market & Competition)

- **시장 규모:** 글로벌 기업 기념품(프로모셔널 프로덕트) 시장은 지속 성장 중이며, 국내 MICE 산업 회복과 구독 경제 확산이 겹쳐 성장 중.
- **경쟁 지형:**
  - *복지몰/제휴몰(기프티쇼 등):* 내부 임직원 대상 중심, 대외 MICE·지역 큐레이션 미커버.
  - *B2B 판촉 도매/턴키(위펀 등):* 물류·턴키 역량은 있으나 담당자가 직접 '검색하고 고르는' 과정이 여전히 필요.
- **포지셔닝:** "복지몰의 접근성, 턴키의 편의에 더해, **대외 고객 기념품을 지역/한국 스토리로 골라주고 알아서 보내주는 구독 큐레이션.**"

---

## 6. 진출 전략 및 로드맵 (GTM)

- **Phase 0 (MVP/검증) — 완료(데모 구현):** Next.js 웹앱으로 프론트엔드 목 데모를 구현하고 Vercel 배포 준비 완료(GitHub 연동). 실제 뮤지엄샵 상품·이미지 반영, 로컬/한국 핏·정렬 필터·주문 방식 2종·예산 장부·ESG 대시보드 시연 가능.
- **Phase 1 (실서비스):** 백엔드/DB/결제/인증 연동, 구독 전환·리텐션 집중, B2B 리드 콘텐츠 마케팅.
- **Phase 2 (확장):** 참여자 개별 다중 배송(엑셀 명단 업로드), 상권 지도, 데이터 기반 자동 추천 알고리즘(현재는 결정론적 공식/무작위 선정).

---

## 7. [개발용] MVP 프론트엔드 개발 스펙 (As-Built PRD)

> **[상태]** 아래 스펙은 이미 구현된 MVP 데모의 실제 구조를 반영한 문서다. 앱은 리포지토리의 `micesolutions/` 폴더에 있으며, 백엔드 없이 **목 데이터(`lib/mockData.ts`) + 클라이언트 상태(Zustand)** 로 동작한다. 참여자 개별 다중 배송 관련 UI/플로우는 이번 MVP에서 제외(Phase 2 보류)했고, 단일 배송지 입력만 구현했다.

### 7.0. Out of Scope (이번 MVP에서 만들지 않는 것)

- 실제 백엔드/DB/API 연동 — 모든 데이터는 `lib/mockData.ts` + 클라이언트 상태로만 처리.
- 실제 결제/PG 연동 — 발주·주문·견적은 화면 알림/텍스트로만 시뮬레이션.
- 실제 인증/회원가입 — `1234` 하드코딩 비밀번호 게이트로 충분.
- 실제 ESG 절감 계산 로직 — `esgScore` 더미 숫자를 가공해 노출.
- Fit 점수·정렬은 결정론적 규칙(if-else) 기반, 자동 큐레이션은 무작위 선정 — 실제 추천/ML은 Phase 2.
- 참여자 개별 다중 배송(엑셀 업로드) — Phase 2 로드맵 항목.
- 다국어/i18n, 다크모드, 접근성 심화.

### 7.1. 기술 스택 및 UI/UX

- **스택:** Next.js(App Router) + TypeScript, Tailwind CSS, **Zustand**(전역 상태), 차트는 **Recharts**(핏 비교 차트, ESG 인포그래픽 — 직접 SVG 금지). shadcn/ui는 미사용, KREAM 톤 **커스텀 경량 컴포넌트**로 구현.
- **보안/배포:** Vercel 배포. `middleware.ts`로 앱 전체에 **비밀번호(`1234`) 잠금 화면(`/gate`)** 적용(인증 쿠키 `gc_auth`).
- **디자인(KREAM):** 배경 화이트 `#FFFFFF`, 본문 `#222222`, 보조 `#6B6B6B`, 경계선 `#EBEBEB`, 이미지 배경 `#F4F4F4`, 포인트 레드 `#EF6253`(CTA 절제 사용), ESG 그린 `#41B979`. Pretendard 서체(CDN, 실패 시 시스템 한글 폰트 폴백).
- **UI 구조:** 모바일 우선(최대 430px), 하단 탭 바 — `[홈]`, `[예산 장부]`, `[ESG 대시보드]`.

### 7.2. 목 데이터 및 상태 구성 (`lib/mockData.ts`)

**상품 14종 — 국립박물관문화상품 실제 데이터, 이미지 전부 로컬(`/public/products/*.jpg`):**

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | 상품 고유 ID (`prod-01`~`prod-14`) |
| `name` | string | 실제 상품명 |
| `price` | number | 실제 단가(원) |
| `moq` | number | 최소 발주 수량 |
| `tags` | string[] | 카테고리/테마 (친환경/IT/프리미엄/로컬/교육/리더십) — 로컬 핏 테마 매칭 + 자동 큐레이션 카테고리 |
| `regionTag` | string \| null | 지역 연고(로컬 핏). 부산/서울/경주/공주/제주/통영, 전국이면 null |
| `image_url` | string | 로컬 경로(`/products/xxx.jpg`) |
| `isPremium` | boolean | VIP(Pro) — true면 홈 피드에서 blur + 잠금 |
| `canPackage` | boolean | 웰컴패키지 가능 여부 |
| `esgScore` | number | 친환경 절감 기여 수치(더미) |
| `koreanCulturalTags` | string[] | 한국 문화 요소(한국 핏) |
| `isTraditionalCraft` | boolean | 전통 공예 여부(한국 핏 가산점) |

**지역별 대표 상품(로컬 핏 대비):** 부산(동백꽃 손수건·분청사기 물고기 가제수건), 경주(신라 금관 손수건·다보탑 키트·취객선비 변색잔), 서울(반가사유상·단청 키보드·대동여지도 노트/한지 편지지), 공주(금동대향로 브론즈/실버), 제주(동자석 인형 키링), 전국(곤룡포 타월/스티커).
**한국 핏 대비:** 곤룡포 비치타월(`koreanCulturalTags:[]` → 최저) vs 대동여지도 한지 편지지(한국 태그 3개 + 전통공예 → 90점 최고).

**사용자/세션 상태 (`initialUserState`) — 듀얼 프로젝트:**

```ts
{
  userName: "이수현", membership: "Pro", remainingTickets: 2,
  activeProjects: [
    { id: "p1", name: "2026 글로벌 리더십 서밋", totalBudget: 5000000, spentBudget: 1750000,
      eventRegion: "부산", eventThemeTags: ["친환경","프리미엄"], audienceType: "mixed" },   // 로컬+한국 핏 노출
    { id: "p2", name: "국내 우수사원 워크숍", totalBudget: 3000000, spentBudget: 800000,
      eventRegion: "경주", eventThemeTags: ["교육","프리미엄"], audienceType: "domestic" },  // 한국 핏 숨김
  ],
}
```
프로젝트를 전환하면 모든 카드의 Fit 점수·정렬·한국 핏 노출 여부가 재계산된다.

### 7.2.1. Fit 점수 계산 (사용자 입력 기반, 결정론적 — `lib/scoring.ts`)

- **로컬 핏(`getLocalFitScore`, 0~100):** 기본 10 + (지역 일치 +50) + (테마 태그 교집합 1개당 +15, 최대 +40), 100 cap.
- **한국 핏(`getKoreanFitScore`, 0~100):** 기본 10 + (`koreanCulturalTags` 1개당 +20, 최대 +60) + (`isTraditionalCraft` +20), 100 cap.
- **노출 규칙:** `audienceType === "domestic"`이면 한국 핏 배지/차트 숨김. `international`/`mixed`일 때만 노출.
- **통합 핏(`getCombinedFitScore`):** 로컬 핏 + (노출 대상일 때만) 한국 핏 → 정렬·기본 노출 순서 기준.
- 홈 카드·상세 모달에서 재사용.

### 7.2.2. [신규] Fit 정렬 필터 (`components/feed/SortFilter.tsx`)

홈 피드(직접 담기 모드) 상단에서 상품 정렬 기준을 고른다.

| 옵션 | 정렬 기준 |
|---|---|
| **통합**(기본) | `getCombinedFitScore` 내림차순 (로컬+한국) |
| **로컬 핏** | `getLocalFitScore` 내림차순 |
| **한국 핏** | `getKoreanFitScore` 내림차순 |

- 동점이면 상품 id로 안정 정렬.
- 국내 전용(domestic) 프로젝트에서는 '한국 핏' 옵션이 **비활성화**되고, 선택돼 있었다면 통합으로 폴백.
- 정렬 로직은 `ProductGrid`가 `sortMode` prop으로 수행.

### 7.3. 주요 화면 명세

- **비밀번호 게이트(`/gate`):** `1234` 입력 → 인증 쿠키 → 홈.
- **홈(피드) 탭 (`/`):**
  - 상단 헤더: 사용자명·멤버십·잔여 티켓(🎫), 데모 초기화 버튼.
  - **기준 프로젝트 선택기(`ProjectSelector`):** 진행 중 프로젝트를 칩으로 전환 → Fit 기준 변경.
  - **주문 방식 스위치(`OrderModeSwitch`):** `[🛒 직접 담기]` / `[🎲 자동 큐레이션]`.
  - 직접 담기 모드: **정렬 필터(SortFilter)** + **상품 2열 그리드(ProductGrid)** + **하단 장바구니 바(CartBar)**. 각 카드는 로컬/한국 핏 배지, VIP blur, 우상단 `＋` 담기 버튼.
  - 자동 큐레이션 모드: **자동 큐레이션 패널(AutoCuratePanel)**.
- **상품 상세 모달(`ProductDetailModal`):** 대표 이미지 + 가격/태그 + `[🎫 티켓 사용해 샘플 받기]`(티켓 차감) + `[🛒 장바구니에 담기]` + 스토리텔링(왜 이 점수인가) + 로컬/한국 핏 비교 차트(Recharts) + `[발주 및 견적 요청]` 폼(패키지 옵션·단일 배송지·비교견적서 자동생성). *(다중 배송은 Phase 2, 미구현·주석 표시)*
- **예산 장부 탭(`/ledger`):** 프로젝트별 예산 프로그레스 + 최근 지출 내역 + `[+ 새 프로젝트]`(지역/테마/참가자 입력 → Fit 기준값). 발주·장바구니 주문·자동 큐레이션 발송이 실시간 예산 차감으로 반영.
- **ESG 대시보드 탭(`/esg`):** `esgScore` 기반 탄소/플라스틱 절감 스탯 타일 + 상품별 ESG 기여 TOP6 가로 막대(Recharts).
- **데모 초기화 버튼:** `initialUserState`로 티켓·예산·프로젝트·장바구니 전체 복원 → 반복 시연.

### 7.4. [신규] 주문 방식 2종 상세

**(A) 직접 담기 (장바구니) — `store.cart`, `CartBar`, `CartSheet`**
1. 상품 카드 `＋` 또는 상세 모달 `장바구니에 담기`로 담는다(기본 수량 = 최소 발주 수량, 재담기 시 수량 합산).
2. 장바구니에 항목이 있으면 하단 탭 바 위에 **CartBar**(품목 수·수량·총액)가 뜬다 → 탭하면 **CartSheet**.
3. CartSheet에서 수량 조정(±10)·삭제·비우기, 합계 확인 후 **주문하기** → `checkoutCart`가 선택 프로젝트 예산에 합계를 반영하고 지출 내역 1건 추가, 장바구니 비움.

**(B) 자동 큐레이션 (랜덤 발송) — `lib/curate.ts` `autoCurate`, `AutoCuratePanel`**
1. 사용자가 **카테고리**(친환경/IT/프리미엄/로컬)와 **종류 수**(2/3/5)를 미리 지정.
2. `[🎲 알아서 보내주기]` → 해당 카테고리 상품 풀에서 무작위(Fisher-Yates) 선정해 보여준다(`다시 뽑기`로 리롤).
3. `[이 구성으로 발송 요청]` → 선정 상품의 (단가×최소수량) 합계를 선택 프로젝트 예산에 반영, 지출 내역에 `자동 큐레이션 · {카테고리} {N}종` 기록.
- 무작위 선정은 하이드레이션 안전을 위해 **사용자 클릭 시점에만** 수행.

### 7.5. [신규] 컴포넌트 구조 (파일 분리 + 한국어 주석)

유지보수를 위해 컴포넌트 파일 1개당 1개, 각 파일 상단에 역할/props/사용처를 설명하는 한국어 주석 블록 + 주요 로직 인라인 한국어 주석.

```
micesolutions/
├─ app/  (page: 홈 / ledger / esg, gate, layout)  · middleware.ts(비번 게이트)
├─ components/
│  ├─ layout/   BottomNav · AppHeader · DemoResetButton
│  ├─ feed/     FeedView · OrderModeSwitch · SortFilter · ProjectSelector · ProductGrid · ProductCard
│  ├─ product/  ProductDetailModal · FitScoreChart · FitScoreBadge · StoryTelling · OrderQuoteForm
│  ├─ cart/     CartBar · CartSheet
│  ├─ ledger/   ProjectBudgetCard · SpendingList · NewProjectForm
│  ├─ esg/      EsgInfographic
│  └─ ui/       Button · Badge · Modal · ProgressBar
└─ lib/  types · mockData · scoring · curate · store · format
```

---

## 8. Vercel 배포 체크리스트 (구현 반영)

- ✅ Next.js App Router `app/` 구조, `middleware.ts` 비밀번호(`1234`) 게이트.
- ✅ 상태는 Zustand 전역 스토어로 관리, 백엔드/DB 없이 `lib/mockData.ts` 초기값만 사용.
- ✅ 상품 이미지 **전부 로컬**(`/public/products`) — 외부 이미지 도메인 설정 불필요, 데모 중 깨짐 없음.
- ✅ 환경변수 없이 동작(`.env.example`만 제공).
- ✅ GitHub 리포지토리 연동 완료(`github.com/isungha8904/micesolutions`, `main`) → Vercel Import 후 기본 설정으로 Deploy(루트 디렉터리 = 리포 루트).
- ✅ `README.md` 포함(로컬 실행 `npm install`/`npm run dev`, 비밀번호 `1234`, Vercel 배포 3~5줄).
- ✅ 페이지 메타데이터(`title`, `description`, OG) 설정.
- ✅ `npm run build` 통과 확인(정적 페이지 생성 + 미들웨어 컴파일).
